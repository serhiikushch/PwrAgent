# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "PwrAgnt"

[setup]
script = '''
__codex_restore_xtrace=""
case $- in
  *x*)
    __codex_restore_xtrace=1
    set +x
    ;;
esac

export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

nvm use --silent

[ -n "$__codex_restore_xtrace" ] && set -x
unset __codex_restore_xtrace

pnpm install
'''

[cleanup]
script = "rm -rf node_modules dist"
