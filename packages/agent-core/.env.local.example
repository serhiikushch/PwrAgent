# Copy this file to .env.local for live xAI-backed smoke coverage.
# The live suite exercises thread continuation and compaction against the
# real xAI Responses API, while the default test suite stays deterministic.
XAI_API_KEY=
XAI_BASE_URL=https://api.x.ai/v1
GROK_MODEL=grok-4.20-reasoning
